node_modules/
.chrome-profile/
# local debug Chrome profile (holds login cookies — NEVER commit)
.discover-profile/
# generated discovery output (scraped third-party data / PII — keep out of git)
discovered*.csv
logs/discover-*.log
logs/discover-cache.json
logs/discover-state-*.json

# per-machine daily action counter (local only)
logs/daily-state.json
